import React from "react";
import styled from "styled-components";
import useFetch from "../useFetch";

const SubContainer = styled.div`
  width: 100%;
  height: 100%;
`;

const SubTitle = styled.div`
  position: relative;
  font-size: 22px;
`;

export default ({ match }) => {
  return <SubTitle>hi</SubTitle>;
};
